PORT=5000
NODE_ENV=development

# Database - PostgreSQL (use docker-compose up to start)
DATABASE_URL="postgresql://imall_user:imall_password_dev@localhost:5432/imall_db?schema=public"

# REQUIRED - Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-here-min-32-chars-required
JWT_EXPIRES_IN=7d

# Database (for future integration)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=imall
# DB_USER=
# DB_PASSWORD=
